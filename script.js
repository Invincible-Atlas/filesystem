// the JSON representation of the file tree that will be used to generate the page
const tree = [{"name":"file1.file"},{"name":"file2.file"},{"name":"folder1","contents":[{"name":"file3.file"}]}]
function generate(tree){
    let fsContainer = document.createElement("div");
    fsContainer.classList = "fsContainer";
    for(i in tree){
        let 
        if(i.contents){

        }
    }
}









